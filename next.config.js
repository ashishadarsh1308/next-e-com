/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["res.cloudinary.com", "example.com"], // Add the domains where your images are hosted
  },
}

module.exports = nextConfig
